# دليل استخدام وتوثيق نظام إدارة العمال - معمل هاشم الأحمدي للتطريز الإلكتروني

## 1. نظرة عامة على النظام (System Overview)
هذا النظام هو تطبيق ويب متكامل ومتقدم لإدارة شؤون العمال، الحضور والانصراف، الرواتب، السلف (السحبيات)، والمصروفات اليومية (الصرفة) لمعمل هاشم الأحمدي للتطريز الإلكتروني. 
يهدف النظام إلى تحويل العمليات اليدوية المعقدة (مثل تسجيل الحركات اليومية من خلال رسائل الواتساب وحساب الرواتب نهاية الشهر) إلى عملية مؤتمتة، دقيقة، وسريعة. 

**التطورات الحديثة:** تم تطوير النظام مؤخراً ليدعم العمل كلياً دون اتصال بالإنترنت (Offline-First/PWA)، وتم تحسين الترحيل الذكي ليعمل عبر تقنيات المطابقة السريعة محلياً دون الحاجة الماسة لخوادم خارجية، بالإضافة إلى دعم إدارة أقسام أو شركات متعددة ضمن نفس الحساب، كما تم تحسينه بشكل كامل ليُستضاف بضغطة زر كـ **موقع ثابت (Static Site)** على منصات مثل Vercel بأقصى سرعة ممكنة.

---

## 2. التقنيات المستخدمة (Technology Stack)
* **واجهة المستخدم (Frontend):** 
  * `React 19` مع `Vite` كبيئة بناء لتوفير أداء فائق السرعة.
  * `Tailwind CSS 4` لتصميم واجهات عصرية ومتجاوبة بالكامل.
  * تطبيق ويب تقدمي `PWA` (عبر `vite-plugin-pwa`) للعمل دون اتصال بالإنترنت (Offline Mode) وإمكانية تثبيت النظام كتطبيق على الهاتف والحاسوب.
* **قاعدة البيانات والتخزين السحابي (Database & Storage):**
  * `Firebase Firestore` مع تفعيل خاصية **(IndexedDB Local Cache)** التي تضمن استمرار عمل النظام وحفظ البيانات عند انقطاع الإنترنت، ثم المزامنة التلقائية فور عودة الاتصال.
* **الذكاء الاصطناعي وخوارزميات الترحيل (Parsing Algorithms):**
  * `Google Gemini 3.5 Flash` متاح عبر خادم خلفي لتحليل النصوص المعقدة.
  * **الخوارزمية المحلية السريعة (Fast Regex Parser):** خوارزمية ذكية متقدمة مبنية بـ TypeScript تعمل بالكامل داخل المتصفح (Client-side) لمعالجة نصوص الحضور واستخراج الأسماء والمبالغ والتأخيرات في أجزاء من الثانية دون استدعاء واجهات برمجية خارجية، مما يناسب بيئات Vercel السريعة.
* **الاستضافة والنشر (Deployment):**
  * مُهيأ تماماً للنشر كـ **تطبيق واجهة أمامية (Static Site)** عبر Vercel. تم حذف الخوادم المعقدة من `package.json` ليعمل كـ `SPA` سريع جداً.

---

## 3. نماذج البيانات (Data Models & Definitions)
### أ. الشركات/الأقسام (Companies)
تم إضافة دعم لعدة شركات/أقسام، حيث يتم عزل بيانات كل شركة عن الأخرى:
* `id`: المعرف الفريد.
* `name`: اسم الشركة أو القسم.
* `createdAt`: تاريخ الإنشاء.

### ب. العمال (Workers)
يمثل كل عامل كياناً في النظام يحتوي على:
* `id`: المعرف الفريد للعامل.
* `workerNumber`: رقم العامل (تسلسلي للتعريف السريع).
* `name`: اسم العامل الكامل.
* `dailyRate`: الراتب اليومي (بالدينار العراقي).
* `jobTitle`: المسمى الوظيفي.
* `status`: حالة العامل (`active` نشط، أو `inactive` غير نشط).
* `joinDate`: تاريخ الانضمام للمعمل.

### ب. السجلات اليومية (Daily Records)
وهي الحركات التي تُسجل لكل عامل في يوم محدد، وتتكون من:
* `id`: معرف السجل.
* `workerId`: معرف العامل المرتبط بهذا السجل.
* `date`: تاريخ الحركة (بصيغة `YYYY-MM-DD`).
* `attendance`: حالة الحضور، وتأخذ إحدى القيم التالية:
  * `full`: حاضر (يوم عمل كامل).
  * `half`: نصف يوم.
  * `absent`: غائب.
* `advancePayment` (السحبيات): مبالغ مالية عامة يأخذها العامل كسلفة وتُخصم من راتبه الإجمالي.
* `allowance` (الصرفة): مبالغ مالية تُعطى للعامل كصرفة يومية (مثل مصروفات شخصية أو أكل) وتُخصم أيضاً من الراتب. تم فصلها عن "السحبيات" لتفصيل كشف الحساب.
* `delayMinutes` (دقائق التأخير): عدد الدقائق التي تأخرها العامل عن بدء الدوام.
* `note`: ملاحظات إضافية نصية على حركة اليوم.

---

## 4. الميزات والصفحات الأساسية وطريقة عملها

### 4.1. لوحة القيادة (Dashboard)
هي الشاشة الرئيسية التي توفر نظرة عامة شاملة وفورية على حالة المعمل لليوم الحالي أو الشهر.
* **الإحصائيات السريعة:** تعرض إجمالي العمال النشطين، عدد الحاضرين اليوم، إجمالي السحبيات والصرفيات لليوم الحالي.
* **الرسوم البيانية:** (إن وُجدت) لتوضيح معدلات الحضور والمصروفات على مدار الأسبوع/الشهر.

### 4.2. إدارة العمال (Workers Page)
* تتيح إضافة عامل جديد مع تحديد اسمه، رقم هاتفه، راتبه اليومي، ومسماه الوظيفي.
* إمكانية تعديل بيانات العامل في حال تغير الراتب أو المسمى.
* إمكانية تغيير حالة العامل إلى "غير نشط" (Inactive) بدلاً من حذفه نهائياً، للحفاظ على سجلاته المالية القديمة دون أن يظهر في قوائم تسجيل الحضور اليومية.

### 4.3. تسجيل الحركات اليومية (Daily Entry)
تسمح هذه الصفحة بإدخال بيانات يوم محدد:
* يقوم المستخدم باختيار التاريخ (الافتراضي هو اليوم).
* يتم عرض قائمة بجميع العمال **النشطين**.
* يمكن للمستخدم تحديد حالة الحضور (حاضر، نصف يوم، غائب)، إدخال مبلغ الصرفة، مبلغ السحبيات، ودقائق التأخير لكل عامل.
* عند النقر على "حفظ"، يتم إرسال البيانات المجمعة إلى قاعدة البيانات كعملية مجمعة (Batch Write) لضمان حفظ كل البيانات معاً في نفس اللحظة (وإذا فشل أحدها، تفشل العملية بأكملها لضمان تناسق البيانات).

### 4.4. الترحيل الذكي والترحيل المتعدد (Smart Chat & Bulk Entry)
* **المشكلة:** قد تصل بيانات العمال على شكل رسائل نصية عبر الواتساب (مثال: "محمد 4000، حسام غياب، علي صرفة 2000")، أو قوائم طويلة لعدة أيام.
* **الحل 1 (المحلل السريع - Fast Parser):** خوارزمية مدمجة `Regex-based` مبرمجة لتعمل فورياً (Offline) في المتصفح. قادرة على فهم اللهجة العراقية للمعمل، وتحويل النصوص والأسماء المعقدة والمبالغ المالية واستخراج التأخيرات دون الحاجة للإنترنت وبسرعة فائقة.
* **الحل 2 (Gemini AI):** عند الحاجة إلى تحليل أعقد، يتوفر نموذج `Google Gemini 3.5 Flash`.
* **الترحيل المتعدد (Bulk Entry):** صفحة مخصصة تتيح للمستخدم لصق قوائم حضور لعدة أيام معاً. يقوم النظام بتقسيمها يومياً، تحليلها عبر الخوارزمية السريعة، عرضها في جداول مبوبة حسب اليوم، واكتشاف الأخطاء أو التكرارات قبل حفظها دفعة واحدة.

### 4.5. كشف الحساب والتقارير (Statements)
وهي أهم ميزة في النظام للمحاسبة الدقيقة نهاية الشهر لكل عامل:
* **تحديد النطاق:** يختار المستخدم الشهر والسنة.
* **جلب البيانات:** يقوم النظام بجلب كل السجلات اليومية الخاصة بالعامل في هذا الشهر.
* **الحسابات الرياضية الدقيقة (Business Logic):**
  * **الراتب المستحق (Total Earned):** 
    يُحسب كالتالي: (أيام الحضور الكامل + (أيام الحضور النصفي × 0.5)) × الراتب اليومي للعامل.
  * **إجمالي الصرفيات (Total Allowances):**
    مجموع كل حقول `allowance` في هذا الشهر.
  * **إجمالي السحبيات (Total Advances):**
    مجموع كل حقول `advancePayment` في هذا الشهر.
  * **إجمالي الخصومات (Total Discounts) بناءً على التأخير:**
    يُحسب التأخير بطريقة دقيقة. النظام يفترض أن يوم العمل يتكون من 12 ساعة، أي 720 دقيقة. 
    *المعادلة:* `مبلغ الخصم = (إجمالي دقائق التأخير ÷ 720) × الراتب اليومي`.
  * **الصافي المتبقي (Net Salary):**
    *المعادلة:* `الراتب المستحق - (إجمالي الصرفيات + إجمالي السحبيات + إجمالي الخصومات)`.

---

### 5. نظام الطباعة الصارم والتصدير (A4 Printing & PDF Export)
تم ضبط النظام بناءً على طلبات صارمة ليتوافق مع الطباعة الورقية وتصدير PDF (بحجم `A4`) بشكل مثالي لتقديمها للعمال، وقد تمت برمجتها عبر أوامر `CSS Media Queries` المتخصصة (`@media print`) ومكتبات مثل `html2canvas` و `jspdf`:
* **تنسيق الصفحة:** تم إخفاء كل العناصر غیر الضروریة للطباعة مثل أشرطة التنقل، الأزرار، والظلال الملونة المزعجة التي تستهلك حبر الطابعة.
* **ترتيب الصفحة (Page Layout):**
  1. **رأس الصفحة (Header):** يظهر شعار واسم الشركة المحددة (مثلاً: "معمل هاشم الاحمدي للتطريز الالكتروني")، اسم العامل، شهر الكشف وتاريخ إصداره.
  2. **جدول التفاصيل اليومية:** جدول مدمج (Compact) يصغر حجم الخطوط فيه ليظهر بوضوح وبأعمدة محددة. تم إخفاء عمود إجراءات التعديل عند الطباعة.
  3. **صف الإجماليات السفلي (Summary Cards):** تتويجاً لتوجيهات النظام، تم وضع بطاقات الحساب في نهاية الصفحة وفي صف واحد تماماً لضمان أن تسعها ورقة A4 واحدة أفقياً.
* **فصل الصفحات (Page Breaks):** عند طباعة كشوفات لعدة عمال في وقت واحد، يستخدم النظام أمر `page-break-before: always`.

---

## 6. الأمان ومزامنة البيانات وبيئة التشغيل
* **الوضع غير المتصل (Offline PWA):** النظام يعتمد على `Firestore IndexedDB Cache`. يعمل التطبيق بكامل طاقته (إضافة عمال، تسجيل حضور، إنشاء كشوفات) حتى لو انقطع الإنترنت. فور عودة الاتصال، تتم مزامنة التغييرات المحلية مع السحابة تلقائياً.
* **الأمان:** تم تأمين مفتاح `GEMINI_API_KEY` داخل الخادم للتعاملات المعقدة.
* **استضافة Vercel فائقة السرعة:** تم تحسين النظام ليتجاوز قيود الخوادم التقليدية. تم حذف سكربتات بدء الخادم المزعجة، وأصبح يُصدّر كـ **Static Site** بالكامل (SPA). يتم تكوين توجيه Vercel حصرياً عبر لوحة تحكم Vercel لتجنب مشاكل `404` عند تحديث الصفحات.

---
**الخلاصة:**
صُمم هذا النظام ليكون "مساعداً آلياً" متطوراً، لا يحتاج للاتصال المستمر بالإنترنت. من لحظة وصول بيانات الحضور المشفرة في رسالة نصية (وتحليلها محلياً بلمح البصر)، إلى فصل الحسابات بين شركات متعددة، وإلى طباعتها وتصديرها كفواتير PDF أنيقة بحجم A4، يقلل النظام من الأخطاء البشرية إلى 0% تقريباً ويوفر ساعات من الجهد المحاسبي للمعمل بأقصى سرعة ممكنة.
