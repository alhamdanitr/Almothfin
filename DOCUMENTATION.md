# دليل استخدام وتوثيق نظام إدارة العمال - معمل هاشم الأحمدي للتطريز الإلكتروني

## 1. نظرة عامة على النظام (System Overview)
هذا النظام هو تطبيق ويب متكامل ومخصص لإدارة شؤون العمال، الحضور والانصراف، والرواتب، والسلف (السحبيات)، والمصروفات اليومية (الصرفة) لمعمل هاشم الأحمدي للتطريز الإلكتروني. يهدف النظام إلى تحويل العمليات اليدوية المعقدة (مثل تسجيل الحركات اليومية من خلال رسائل الواتساب وحساب الرواتب نهاية الشهر) إلى عملية مؤتمتة، دقيقة، وسريعة. يتميز النظام بقدرته على استخدام الذكاء الاصطناعي لتحليل النصوص وتحويلها إلى بيانات مالية دقيقة، بالإضافة إلى إمكانية طباعة كشوفات حساب احترافية للعمال.

---

## 2. التقنيات المستخدمة (Technology Stack)
* **واجهة المستخدم (Frontend):** 
  * `React 19` مع `Vite` كبيئة بناء لتوفير أداء فائق السرعة.
  * `Tailwind CSS 4` لتصميم واجهات عصرية ومتجاوبة بالكامل مع مختلف الشاشات.
  * `Lucide React` للأيقونات التوضيحية.
  * `Date-fns` لمعالجة وتنسيق التواريخ.
* **الخادم (Backend):**
  * `Node.js` مع إطار عمل `Express`.
  * `esbuild` لتجميع الخادم (Bundling) وجعله ملفاً واحداً جاهزاً للتشغيل في بيئة الإنتاج.
* **قاعدة البيانات (Database):**
  * `Firebase Firestore` لتخزين البيانات السحابية بشكل فوري (NoSQL) لضمان عدم ضياع أي بيانات.
* **الذكاء الاصطناعي (AI):**
  * `Google Gemini 3.5 Flash` من خلال `@google/genai` لتحليل النصوص العربية الواردة من إدارة المعمل وتحويلها إلى سجلات يومية (الترحيل الذكي).

---

## 3. نماذج البيانات (Data Models & Definitions)

### أ. العمال (Workers)
يمثل كل عامل كياناً في النظام يحتوي على:
* `id`: المعرف الفريد للعامل.
* `workerNumber`: رقم العامل (تسلسلي للتعريف السريع).
* `name`: اسم العامل الكامل.
* `dailyRate`: الراتب اليومي (بالدينار العراقي).
* `jobTitle`: المسمى الوظيفي.
* `status`: حالة العامل (`active` نشط، أو `inactive` غير نشط).
* `joinDate`: تاريخ الانضمام للمعمل.

### ب. السجلات اليومية (Daily Records)
وهي الحركات التي تُسجل لكل عامل في يوم محدد، وتتكون من:
* `id`: معرف السجل.
* `workerId`: معرف العامل المرتبط بهذا السجل.
* `date`: تاريخ الحركة (بصيغة `YYYY-MM-DD`).
* `attendance`: حالة الحضور، وتأخذ إحدى القيم التالية:
  * `full`: حاضر (يوم عمل كامل).
  * `half`: نصف يوم.
  * `absent`: غائب.
* `advancePayment` (السحبيات): مبالغ مالية عامة يأخذها العامل كسلفة وتُخصم من راتبه الإجمالي.
* `allowance` (الصرفة): مبالغ مالية تُعطى للعامل كصرفة يومية (مثل مصروفات شخصية أو أكل) وتُخصم أيضاً من الراتب. تم فصلها عن "السحبيات" لتفصيل كشف الحساب.
* `delayMinutes` (دقائق التأخير): عدد الدقائق التي تأخرها العامل عن بدء الدوام.
* `note`: ملاحظات إضافية نصية على حركة اليوم.

---

## 4. الميزات والصفحات الأساسية وطريقة عملها

### 4.1. لوحة القيادة (Dashboard)
هي الشاشة الرئيسية التي توفر نظرة عامة شاملة وفورية على حالة المعمل لليوم الحالي أو الشهر.
* **الإحصائيات السريعة:** تعرض إجمالي العمال النشطين، عدد الحاضرين اليوم، إجمالي السحبيات والصرفيات لليوم الحالي.
* **الرسوم البيانية:** (إن وُجدت) لتوضيح معدلات الحضور والمصروفات على مدار الأسبوع/الشهر.

### 4.2. إدارة العمال (Workers Page)
* تتيح إضافة عامل جديد مع تحديد اسمه، رقم هاتفه، راتبه اليومي، ومسماه الوظيفي.
* إمكانية تعديل بيانات العامل في حال تغير الراتب أو المسمى.
* إمكانية تغيير حالة العامل إلى "غير نشط" (Inactive) بدلاً من حذفه نهائياً، للحفاظ على سجلاته المالية القديمة دون أن يظهر في قوائم تسجيل الحضور اليومية.

### 4.3. تسجيل الحركات اليومية (Daily Entry)
تسمح هذه الصفحة بإدخال بيانات يوم محدد:
* يقوم المستخدم باختيار التاريخ (الافتراضي هو اليوم).
* يتم عرض قائمة بجميع العمال **النشطين**.
* يمكن للمستخدم تحديد حالة الحضور (حاضر، نصف يوم، غائب)، إدخال مبلغ الصرفة، مبلغ السحبيات، ودقائق التأخير لكل عامل.
* عند النقر على "حفظ"، يتم إرسال البيانات المجمعة إلى قاعدة البيانات كعملية مجمعة (Batch Write) لضمان حفظ كل البيانات معاً في نفس اللحظة (وإذا فشل أحدها، تفشل العملية بأكملها لضمان تناسق البيانات).

### 4.4. الترحيل الذكي (Smart Entry AI)
* **المشكلة:** قد تصل بيانات العمال على شكل رسائل نصية عبر الواتساب (مثال: "محمد 4000، حسام غياب، علي صرفة 2000").
* **الحل:** ميزة الترحيل الذكي تستخدم ذكاء `Google Gemini` عبر خادم `Node.js` الآمن.
* **آلية العمل الدقيقة:**
  1. يقوم المستخدم بلصق النص العشوائي في نافذة الترحيل الذكي.
  2. يتم إرسال النص إلى مسار `API` الخادم المسمى `/api/parse-attendance`.
  3. يقوم الخادم بتمرير النص إلى نموذج `Gemini 3.5 Flash` مع **أوامر دقيقة جداً (System Prompt)**:
     * **حالة الحضور:** يبحث عن كلمات مثل "غياب"، "نصف يوم"، "داوم ساعتين" ويحولها لـ `absent` أو `half` أو `full`.
     * **السحبيات (Advance Payments):** أي رقم يأتي بجانب اسم العامل يُعتبر سحبيات (مثلاً: "محمد 4000" تعني سحبيات 4000).
     * **الصرفة (Allowances):** إذا ذُكرت كلمة "صرفة" صراحةً قبل الرقم، تُعتبر صرفة (مثلاً: "محمد صرفة 2000").
     * **التأخير:** يحول العبارات مثل "تأخير ساعة ونص" إلى 90 دقيقة.
     * **التاريخ:** يبحث عن أي تاريخ في النص، وإلا يستخدم تاريخ اليوم الافتراضي.
     * **مطابقة الأسماء:** يجب أن يُطابق الذكاء الاصطناعي الاسم في النص مع قائمة العمال المرسلة إليه لإرجاع الـ `workerId` الدقيق.
  4. يُرجع الخادم استجابة `JSON` مهيكلة (Structured Output).
  5. تُعرض البيانات في جدول للمراجعة، وإذا تأكد المستخدم من صحتها يتم حفظها في قاعدة البيانات.

### 4.5. كشف الحسا�� والتقارير (Statements)
وهي أهم ميزة في النظام للمحاسبة الدقيقة نهاية الشهر لكل عامل:
* **تحديد النطاق:** يختار المستخدم الشهر والسنة.
* **جلب البيانات:** يقوم النظام بجلب كل السجلات اليومية الخاصة بالعامل في هذا الشهر.
* **الحسابات الرياضية الدقيقة (Business Logic):**
  * **الراتب المستحق (Total Earned):** 
    يُحسب كالتالي: (أيام الحضور الكامل + (أيام الحضور النصفي × 0.5)) × الراتب اليومي للعامل.
  * **إجمالي الصرفيات (Total Allowances):**
    مجموع كل حقول `allowance` في هذا الشهر.
  * **إجمالي السحبيات (Total Advances):**
    مجموع كل حقول `advancePayment` في هذا الشهر.
  * **إجمالي الخصومات (Total Discounts) بناءً على التأخير:**
    يُحسب التأخير بطريقة دقيقة. النظام يفترض أن يوم العمل يتكون من 12 ساعة، أي 720 دقيقة. 
    *المعادلة:* `مبلغ الخصم = (إجمالي دقائق التأخير ÷ 720) × الراتب اليومي`.
  * **الصافي المتبقي (Net Salary):**
    *المعادلة:* `الراتب المستحق - (إجمالي الصرفيات + إجمالي السحبيات + إجمالي الخصومات)`.

---

## 5. نظام الطباعة الصارم (A4 Printing System)
تم ضبط النظام بناءً على طلبات صارمة ليتوافق مع الطباعة الورقية (بحجم `A4`) بشكل مثالي لتقديمها للعمال، وقد تمت برمجتها عبر أوامر `CSS Media Queries` المتخصصة (`@media print`):
* **تنسيق الصفحة:** تم إخفاء كل العناصر غیر الضروریة للطباعة مثل أشرطة التنقل، الأزرار، والظلال الملونة المزعجة التي تستهلك حبر الطابعة.
* **ترتيب الصفحة (Page Layout):**
  1. **رأس الصفحة (Header):** يظهر شعار/اسم "معمل هاشم الاحمدي للتطريز الالكتروني"، اسم العامل، شهر الكشف وتاريخ إصداره.
  2. **جدول التفاصيل اليومية:** جدول مدمج (Compact) يصغر حجم الخطوط فيه ليظهر بوضوح وبأعمدة محددة: التاريخ، اليوم، الحضور، الصرفة، السحبيات، التأخير، الملاحظات. (تم إخفاء عمود إجراءات التعديل عند الطباعة).
  3. **صف الإجماليات السفلي (Summary Cards):** 
     تتويجاً لتوجيهات النظام، تم وضع بطاقات الحساب (الراتب المستحق، إجمالي الصرفيات، الخصومات، الصافي المتبقي) **في نهاية الصفحة وفي صف واحد تماماً (Single Row)**. تم ضبط المسافات (`gap-1` و `grid-cols-5`) وتقليص حجم الخطوط لضمان أن تسعها ورقة A4 واحدة أفقياً في الأسفل مهما كان عدد أيام الشهر.
* **فصل الصفحات (Page Breaks):** عند طباعة كشوفات لعدة عمال في وقت واحد، يستخدم النظام أمر `page-break-before: always` لضمان أن كل عامل يحصل على كشف حسابه في ورقة A4 منفصلة ومستقلة تماماً.

---

## 6. الأمان ومزامنة البيانات
* النظام مبني للعمل بتنسيق (Full-Stack). الواجهة (React) تتواصل مع قاعدة بيانات `Firestore` للبيانات العادية، بينما يتولى مسار Vercel Function `/api/parse-attendance` المهام الحساسة مثل الاتصال بـ `Gemini`.
* تم تأمين مفتاح `GEMINI_API_KEY` داخل بيئة Vercel الخادمية فقط ولا يتم إرساله إطلاقاً إلى متصفح المستخدم. يجب إضافة المتغير من إعدادات المشروع في Vercel لكل بيئة تريد تشغيل الميزة فيها.
* عند النشر على Vercel يستخدم المشروع `npm run build` لبناء واجهة Vite داخل `dist`، بينما يعمل `api/parse-attendance.ts` كدالة Serverless مستقلة. للتطوير المحلي يبقى `server.ts` هو نقطة التشغيل عبر `npm run dev`.
* خطوات الربط: افتح إعدادات مشروع Vercel، أضف `GEMINI_API_KEY` بقيمة مفتاح Gemini، فعّل البيئات المطلوبة (Preview وProduction)، ثم أعد النشر. لا تضع المفتاح داخل ملفات `src` أو أي متغير يبدأ بـ `VITE_`.

---
**الخلاصة:**
صُمم هذا النظام ليكون "مساعداً آلياً" وليس مجرد دفتر حسابات. من لحظة وصول بيانات الحضور المشفرة في رسالة نصية، إلى تحويلها لأرقام دقيقة، وإلى طباعتها كفاتورة أنيقة بحجم A4 في نهاية الشهر، يقلل النظام من الأخطاء البشرية إلى نسبة تقارب الصفر% ويوفر ساعات من الجهد المحاسبي للمعمل.
